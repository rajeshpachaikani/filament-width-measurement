import cv2
import numpy as np



class FilaMeasure:
    def __init__(self):
        print("Created Class Instance")
    
    def nothing():
        print("NNNNN")

    def get_measurement(img):
        if img.shape[0] > 0:
            print("Image shape: " + str(img.shape))
            # img_cln = img[:, 80:80+480]
            # cv2.imshow("s",img_cln)
            # cv2.waitKey(1)
            # cv2.imshow("s",img)
            # cv2.waitKey(1)
            # cv2.imshow("s",img_cln)
            # cv2.waitKey(1)
            # cv2.imshow("s",img)
            # cv2.waitKey(1)
            # cv2.imshow("s",img_cln)
            # cv2.waitKey(1)
            # cv2.imshow("s",img)
            # cv2.waitKey(1)
            # cv2.imshow("s",img_cln)
            # cv2.waitKey(1)
            # cv2.imshow("s",img)
            # cv2.waitKey(1)
            # cv2.imshow("s",img_cln)
            # cv2.waitKey(1)
            # cv2.imshow("s",img)
            # cv2.waitKey(1)
            # cv2.imshow("s",img_cln)
            # cv2.waitKey(1)
            # cv2.imshow("s",img)
            # cv2.waitKey(1)
            # cv2.imshow("s",img_cln)
            # cv2.waitKey(1)
            # cv2.imshow("s",img)
            # cv2.waitKey(1)
            # cv2.imshow("s",img_cln)
            # cv2.waitKey(1)
            # cv2.imshow("s",img)
            # cv2.waitKey(1)
            # cv2.imshow("s",img_cln)
            # c